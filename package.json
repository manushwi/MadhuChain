{
  "name": "madhuchain",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:beekeeper": "npm run start --workspace beekeeper-app",
    "dev:factory": "npm run start --workspace factory-app",
    "dev:consumer": "npm run dev --workspace consumer-web",
    "dev:admin": "npm run dev --workspace kvic-dashboard"
  },
  "engines": {
    "node": ">=20"
  }
}
