# KVIC Dashboard

Server-rendered administration console for consortium membership, deterministic
telemetry assessments, alerts, Fabric events, proof verification, and audit.

Set `BACKEND_URL`, seed an administrator with `ADMIN_SEED_EMAIL` and
`ADMIN_SEED_PASSWORD`, then run `npm run dev --workspace kvic-dashboard`.
